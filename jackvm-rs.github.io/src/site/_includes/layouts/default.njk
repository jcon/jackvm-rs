<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="{{ '/img/favicon.ico' | url }}">
    <link rel="stylesheet" href="{% webpackAsset 'main.css' %}">
    <title>{{ title }}</title>
  </head>
  <body class="flex flex-col items-center">
      <div class="bg-white flex flex-col space-x-4 space-y-4 items-center max-w-3xl">
        <h1 class="text-6xl text-blue-400 font-semibold font-serif"><a href="{{ '/' | url }}">JackVM-rs</a></h1>
        <h2 class="text-lg text-blue-400 font-thin">A demonstration site showcasing the <a href="#">jackvm-rs project</a>.</h2>

        <p>
          The JackVM is a 16-bit stack based virtual machine that runs programs compiled from
          the Jack language. Jack is a simple Java-like language that for which you build a
          compiler in <a class="{% page_link %}" href="https://www.nand2tetris.org">nand2tetris</a>. Even though Jack is a small language, you can create fairly
          powerful programs.
        </p>


        {{ content | safe }}
      </div>
  </body>
</html>
